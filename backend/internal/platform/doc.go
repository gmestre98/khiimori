// Package platform holds shared infrastructure and cross-cutting helpers
// (config, logging, database handles). It is not a domain module and may be
// imported by any domain module; domain modules must not import each other.
package platform
